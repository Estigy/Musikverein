<?php
$title = 'Kurse';
$this->headTitle($title);
?>
<h1>
    <?php echo $this->escapeHtml($title); ?>
    <a class="btn btn-success pull-right" href="<?php echo $this->url('workshopEdit', array('id' => 0));?>">Neuen Kurs hinzufügen</a>
</h1>

<table class="table table-striped table-hover">
    <thead>
	    <tr>
            <th>Datum</th>
            <th>Name</th>
            <th>Ort</th>
            <th>&nbsp;</th>
	    </tr>
    </thead>
	<?php
    foreach ($workshops as $workshop) {
        ?>
        <tr>
            <td>
                <?php
                echo $this->escapeHtml($workshop->beginDate->format('d. m. Y'));
                if ($workshop->endDate) {
                    echo ' bis ' . $this->escapeHtml($workshop->endDate->format('d. m. Y'));
                }
                ?>
            </td>
            <td><?php echo $this->escapeHtml($workshop->name);?></td>
            <td><?php echo $this->escapeHtml($workshop->city);?></td>
            <td>
                <a href="<?php echo $this->url('workshopEdit', array('id' => $workshop->id));?>">Bearbeiten</a>
            </td>
        </tr>
        <?php
    }
    ?>
</table>
